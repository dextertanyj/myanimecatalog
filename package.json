{
  "name": "myanimecatalog",
  "version": "0.1.0",
  "main": "index.js",
  "author": "Dexter Tan",
  "license": "MIT",
  "private": false,
  "scripts": {
    "dev:client": "docker exec -it $(docker-compose ps -q client) bash",
    "dev:server": "docker exec -it $(docker-compose ps -q server) sh",
    "dev:db": "docker exec -it $(docker-compose ps -q db) bash"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
